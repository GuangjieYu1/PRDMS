## Agent skills

### Issue tracker

Issues and specs live in the GitHub repo's Issues, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout. See `docs/agents/domain.md`.
